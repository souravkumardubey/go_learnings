package main

import "fmt"

func main() {
	// var name string
	// const name = "Sourav";
	// fmt.Println(name)

	// ? multiple constants
	const (
		port = 5000
		host = "localhost"
	)
	fmt.Println(port)
}